import { queueElement, fileDatabase} from "../types"

export async function httpRequest(request: object){
	/**
	 * General function to make a http request and, if successful, output response. This function is intended to work similarly to
	 * my 'getPaginatedQualtricsData' function in apps script. However, now I kind of doubt whether (a) it can be general enough to meet all needs and
	 * (b) if it's wise to try and force all http requests to work in a single function. Maybe instead I should create a pure http request function and then
	 * create custom functions for each input request type.
	 */
	return []
}


